function affKeyCode(event) {
  document.getElementsByTagName("input")[0].value+=event.key;
};

  // Enregistrer la touche
  // Tester si le code-barre est complet



/*
- enegistrer les frappe au clavier
- récupérer plusieurs frappes pour faire un code barre
- détecter la fin d'un code barre
- remplir un champs avec le code barre
*/
